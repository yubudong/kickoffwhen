# Hetzner 已上线版本与听写功能整合

目标：在现有隔离听写工作区保留 a9cd2d7 的全部听写能力，同时纳入已上线 recording1 源码；本阶段不发布、不修改生产数据、不推送。

基线：部署工作区 `/Users/yubudong/.codex/worktrees/1540/学习管理网站` 的 ad1f066 + 未提交的生产相关源码；只读，不能修改其文件或 Git 状态。听写工作区 `/Users/yubudong/.codex/worktrees/grade5-dictation/学习管理网站`，a9cd2d7。两者代码共同基线 ad1f066（a6f19d0 只增加听写设计/计划）。

全局约束：不读取或复制任何真实环境文件/密钥/媒体/数据库；不复制 .DS_Store、.pnpm-store、node_modules 或构建产物；保留当前生产注册限制、SMTP、Azure 语速/限速、健康检查、待办、录音和部署文件。不运行外部发信、真实 TTS 或生产写入。所有手工编辑用 apply_patch。Next 代码编辑前读本地版本文档。任何新修复先写失败测试。

### Task 1: 整合生产功能并消除迁移冲突

1. 检查两个工作区 Git 状态，记录部署源文件清单与摘要（无秘密内容）。从部署工作区导入相对于 ad1f066 的已跟踪变更及明确相关的未跟踪源码、测试、部署模板、runbook。不改动源工作区；不要覆盖听写修改。共享文件三方合并，冲突手工解决。不要将部署源未提交工作提交回其原分支。
2. 保留已上线 `0019_bizarre_venus.sql` 及其快照、journal 项字节不变。将听写迁移变为 0020；由包含 todos 与听写的完整 schema 生成新快照与 journal，确认只增加教材小节及卡片元数据，没有删表或重复 todos 创建。旧的听写 0019 文件从候选分支移除（原提交仍可追溯）。
3. 冲突重点：audio-sequence 的暂停/重听逻辑必须保留，同时使用已合成语速，不二次加速；task/session 的待办创建/提交钩子与科目筛选并存；schema 包含 todos 与 textbookSections；globals.css 保留双方样式；单测保留双方断言。
4. 添加组合回归：带 subject 的听写创建仍创建待办，提交仍推进待办；音频新流程仍仅正常倍率播放。验证已有内容审计仍为 312 条，247 必留，12/41 替换，223 所属课次写字字符位覆盖。
5. 运行 unit、lint、typecheck、seed validate-only。独立全新测试数据库中测试迁移 0000→0019→0020（包含已有任务/待办的升级保留检查），全量 integration；再运行 build 与 Chromium/WebKit dictation E2E。不得重用生产数据库；不得删除旧测试数据库。
6. 更新 `docs/runbooks/dictation-integration.md`：源版本、整合方式、迁移顺序、验证证据、部署前事项、回退只退应用而保留新增表/历史。说明本地候选未上线。更新听写进度文档，不篡改旧历史。
7. 自审并仅提交本次整合相关改动到现有听写分支；不推送、不合并、不部署。报告测试命令、输出、冲突处理、文件清单、未验证项。旧8处media动态追踪警告若仍在，准确报告。

完成后由控制代理独立审查整合任务与最终候选，最终向 Tom 汇报并请求部署确认。
