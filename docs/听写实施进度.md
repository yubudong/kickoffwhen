# SDD ledger — plan: docs/superpowers/plans/2026-09-22-grade5-dictation-content.md

工作区：独立工作区 grade5-dictation；起点 a6f19d0。

## 实施与审查记录

- Task 1–3: backend 子代理实施；主代理独立核对内容输入；source_audit 子代理只读复核原图。
- Task 1–3 review: backend_review发现P1审计未接入写库门禁、P2小节互换顺序撞唯一约束；修复轮1进行中。全量单测134通过，但不据此跳过审查缺陷。
- Task 1–3: complete（未提交工作区改动；修复轮1的P1/P2均经独立复审ADDRESSED，review clean）。主代理完整集成回归19文件132测试通过。
- Task 4: content实施中，brief /tmp/grade5-content-brief.md；先内容测试RED，再填独立基准/最终词库/逐条核对稿。
- Task 4: 内容实施完成，独立content_review复核中。全量单测40文件138通过；主代理在自动回滚测试事务导入真实312条两次，结果首次inserted312/updated0，第二次inserted0/updated312，退出0且已回滚。
- Task 4: complete（未提交；独立content_review给出Spec PASS、Quality PASS，无阻断项）。报告/tmp/grade5-content-review-report.md。
- Task 5–7: ui代理实施中，brief /tmp/grade5-ui-brief.md；关注科目全链路、提示题号同步和播放控制竞态。
- Task 5–7: complete（未提交；ui_review Spec PASS、Quality PASS）。相关单测29、集成23、双浏览器4通过；主代理已目视截图无答案泄露。
- Task 5–7: minor (deferred): 可补充保存Promise未返回时取消播放的专门回归；现有run身份校验与pending/retryLocked已审查。
- Task 1–3: minor (deferred): JSON Schema未完整表达Zod中文条件规则，运行时使用Zod+审计门禁；最终review复核。
- Task 8: 全量unit41文件150通过；integration19文件136通过；lint/typecheck退出0；build退出0，保留8处既有media/store动态文件追踪警告。最初build缺SMTP占位配置，补本机SMTP占位后成功，未发邮件。
- Task 8: 默认并发integration曾有1个旧全局限流表清理用例失败；串行136通过，已将集成文件串行配置，默认命令136通过。独立最终review将检查此测试隔离修复。
- Task 8: final_review正在审查全分支，/tmp/grade5-final-review.diff；Next自动生成的额外tsconfig include已还原，不混入业务改动。
- Task 8: 最终审查唯一P2进入一次修复波次：同内置词条编号修改答案会破坏旧任务的音频/批改一致性；backend实施，随后限定复审。
- Task 8: complete（未提交工作区改动；最终唯一修复波次1项ADDRESSED、0项open，独立复审Ready）。最终完整integration19文件138项通过，unit41文件150项通过，lint/typecheck/build成功，双浏览器4项通过；build仍有8处原有警告。报告/tmp/grade5-final-rereview-report.md。
- 交付边界：保留隔离worktree；尚未合并、推送、部署或导入正式数据库；真实TTS发音与音质仍需上线前验收。英语内容等待实际教材，不编造词库。

Ruling: 同builtinKey的answerText、broadcastText、subject视为不可变身份，变更拒绝并回滚该教材事务；拼音、语境等元数据仍可更新。避免旧任务音频和新答案混用；代价是改词必须使用新key并保留旧卡历史。

## 已验证基线

- Node 24.15.0；`pnpm_config_verify_deps_before_run=false` 避免共享依赖被重装。
- 原始单元测试：37 文件、125 测试通过。
- 后端完成后浏览器基线：Chromium+WebKit 4/4通过。原先Turbopack因跨工作区node_modules软链启动失败，已克隆本地依赖解决；保留旧软链于/tmp/grade5-dictation-node-modules-link。运行有NO_COLOR/FORCE_COLOR环境警告，后续清理环境变量消除。
- 独立测试数据库：Docker `grade5-dictation-tests-db-1`，仅使用 `family_learning_test`。
- 来源独立复核：247 必留词，41 原辅助词，223 写字字符位；兴亡盛衰合并后原稿为 288 条。
- 缺失写字覆盖：第8课禽，第11、12、20、23课全部字，第24课描。新增条目与替换分开计算。
- /tmp/grade5-source-audit.md 保存逐课独立基准；/tmp/grade5-context-draft.json 正在独立编辑原创语境，不修改仓库实现。

- 内容草稿完成：312条，247必留、41原辅助（12替换）、24新增；主代理通读所有语境，修正4处表达。
- 迁移0019已仅应用隔离测试库；其余集成回归122通过/1失败。失败为旧OCR测试固定过期日2026-09-09与真实当前日期冲突，注入测试时钟修正，不改变业务过期逻辑。

Ruling: 保留原始转录稿作为288条修正后的来源记录，另输出312条最终内置词库核对稿，避免改写结果与原图混淆；代价是多维护一份面向人的导出文档。

Ruling: 共享数据库的集成测试按文件串行执行，避免全局维护用例互相干扰；代价是完整集成测试从约3秒增加到约10秒，不改变生产逻辑。

## 计划预检

| 任务/共享接口 | 检查与裁定 |
| --- | --- |
| 1 → 2、3、5、6 | sectionId/pinyinText/curriculumSource/sourceOrder 需一致；先完成数据库基础 |
| 2 → 3、4 | units.sections.cards 为统一种子结构，保留英语空 units 兼容 |
| 3 → 4 | 导入前必须完成词表审计 |
| 4 → 5、6 | 字词内容不直接暴露于听写阶段 API |
| 5 → 7 | 科目过滤要同时防止到期复习混入其他科目 |
| 6 → 7 | 当前词提示必须和当前实际播放一致，而非仅按已播放长度计算 |
| 1 | 表结构增加同时更新映射与迁移；测试数据库需独立 |
| 2 | 审计预期不能由种子自身推导；以照片转录独立基准比较 |
| 3 | 幂等更新只处理内置词；应用数据库前校验所有输入 |
| 4 | 原稿未包含 11/12/20/23 课写字条目，须独立补齐 |
| 5 | 三层筛选控制新词，同时科目约束复习词 |
| 6 | 语境复用 hintText，只安全投影拼音和语境 |
| 7 | 暂停重复轮次/间隔；重听不能推进题号 |
| 8 | 无真实语音凭据时报告限制，不把静音测试当作真实语音验收 |

Ruling: 原计划的替换比例只数 adaptation 标志不够，应与原补充词基准逐项比较；新增写字覆盖条目不进入原补充词替换率分母。否则会有错误统计。

Ruling: 写字表覆盖按所属课次检查，避免别课的同字掩盖缺漏；缺少课次补充原创词，不冒称原有内容。

Ruling: 子代理按数据库基础、内容、界面三个边界串行实施，共享接口集中复核；独立只读来源审计可并行。

Ruling: 改写 20%–40% 是用户指定编辑目标，不能作为版权免责保证；审计报告只陈述内容和覆盖事实。
